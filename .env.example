# Required: your OpenAI API key
OPENAI_API_KEY=

# Optional: override the model used for /api/ask (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Optional: shows a "Demo" badge in the UI to indicate sample content (default: false)
DEMO_MODE=false
